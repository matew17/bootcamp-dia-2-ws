import { AtomicoAnimationDirective } from './atomico-animation.directive';

describe('AtomicoAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new AtomicoAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
